# Tasks

## 1. Failing contracts (TDD — no production code yet)

- [ ] 1.1 Flip 睡了么 frontend contracts in `frontend/src/pages/Shuileme.test.tsx`, `frontend/src/utils/shuilemeSound.test.ts`, and a new `frontend/src/utils/queueLoungePhotos.test.ts`: six `.sm-dock` buttons including 聊; chat dock has `textarea`; delayed mock `POST /shuileme/chat` shows `role="status"` then a dry lecture without `you have` / `你患有`; no fetch to `/lalem/`; 醒了 freezes 已躺 and stops sound; sound tap marks playing and 停 stops; `getShuilemeSoundState` is `playing` after start without closing the reused AudioContext; three-card gallery paints titles immediately with only the first two `src` set until `load`; sheet hero is `/shuileme/beds/*.jpg`; no `video` / `Notification` / wikipedia `<a>` / `#c6a56a`. Use `Array.from`. Verify `cd frontend && CI=true npx react-scripts test --watchAll=false --testPathPattern='Shuileme.test|shuilemeSound|queueLoungePhotos|shuilemeAbsence'` **fails** while the sixth dock, stop, sequential queue, thinking row, and sound stop UI are missing
- [ ] 1.2 Flip 拉了么 frontend contracts in `frontend/src/pages/Lalem.test.tsx`: header 完了; delayed mock chat POST shows colorful `role="status"` and `aria-busy`; after 完了 the 已坐 value stays frozen and a further 5-minute visible wait does not show 久坐警报 or a new companion; sit-alert still stacks when 完了 was **not** tapped. Verify `cd frontend && CI=true npx react-scripts test --watchAll=false --testPathPattern='Lalem.test'` **fails** while stop and thinking are missing
- [ ] 1.3 Flip Go contracts in `backend/internal/ai/shuileme_catalog_test.go`, new `backend/internal/ai/shuileme_chat_test.go`, and `backend/internal/api/shuileme_handlers_test.go`: beds/rooms unique JPEG SOI hashes (reuse `assertLalemLocalJPEG` / `sha256Hex`); `POST /api/v1/shuileme/chat` with `{locale,message}` returns 200 JSON `reply`; canned when no `CompleteFn`; `you have` / `你患有` replaced by canned; reply must not contain 拉了么 poop canned strings such as `便便科普`. Verify `cd backend && go test ./internal/ai ./internal/api -count=1` **fails** while unique hashes and the chat route are missing

## 2. Photograph pack

- [ ] 2.1 Replace geometric stills under `frontend/public/shuileme/beds/` and `frontend/public/shuileme/rooms/` with unique real Wikimedia Commons / public-domain JPEGs (one distinct photograph per catalog id). Rewrite `frontend/public/shuileme/ATTRIBUTION.md` as a Commons table like `frontend/public/lalem/ATTRIBUTION.md`. Update credits in `backend/internal/ai/shuileme_beds.json` and `shuileme_rooms.json`. Leave `lore/` illustrated. Verify 1.3 unique-hash tests pass for beds/rooms and no `imageUrl` contains `://`

## 3. Chat API and sound engine

- [ ] 3.1 Add `AdviseShuilemeChat` (SiliconFlow `CompleteFn`, dry law/science/math prompt, parse `{reply}` or prose, ≥8 canned dry lines per locale, strip diagnosis, never 拉了么 funny poop copy). Wire `POST /api/v1/shuileme/chat` with a **separate** limiter from 拉了么 chat. Verify 1.3 Go chat tests pass and existing `POST /api/v1/lalem/chat` tests still pass
- [ ] 3.2 Change `frontend/src/utils/shuilemeSound.ts` to reuse one AudioContext, call `resume()` synchronously on the tap (no `stop()`/`close()` before resume), keep the generation token, gain ~0.45, export `getShuilemeSoundState(): 'idle' | 'playing'`. If the buffer graph throws, fall back to local `/shuileme/sounds/{scene}.wav` via `Audio` (package tiny loops if needed). Verify 1.1 `shuilemeSound.test.ts` passes: resume on start, second start does not require a new closed context, unmount/stop still silences, `visibilitychange` does not stop

## 4. Pages: stop, sequential photos, docks, thinking

- [ ] 4.1 Add `queueLoungePhotos` in `frontend/src/utils/queueLoungePhotos.ts` and use it on 睡了么 beds/rooms galleries: reserved `.sm-ph` boxes, first two `src` + `fetchpriority=high` + `loading=eager`, later `src` only after previous `load`/`error`. Paint cards as soon as catalog JSON arrives. Verify 1.1 sequential gallery tests pass
- [ ] 4.2 睡了么 page: header 醒了 / I'm up (freeze 已躺, `stopShuilemeSound`, stay on `/shuileme`); dock `chat` (i18n 聊 / Chat) with transcript + textarea posting to `/shuileme/chat` only; sleepy `.sm-think` `role="status"` `aria-live="polite"` and transcript `aria-busy`; reduced-motion static; sound dock 正在响 + 停; CSS `html.sm-world .sm-dock` `repeat(6, minmax(0,1fr))` wrap `<339px`. Verify 1.1 Shuileme tests pass
- [ ] 4.3 拉了么 page: header 完了 / Done sitting (`sessionOver` freezes 已坐, clears sit-alert + companion, skips later sit/companion effects, stays on `/lalem`); chat log `.ll-think` colorful thinking status while `chatBusy`; `#c6a56a` still absent. Verify 1.2 Lalem tests pass and existing sit-alert-without-stop tests still pass

## 5. Isolation

- [ ] 5.1 Isolation: no officer Navigation / Fridge Raid / landing restyle; 睡了么 still has no `/lalem/` client calls; no `Notification` / `requestPermission`; no `<video>`; no `#c6a56a` / `#ff4d8d`; do not archive `shuileme-sleep-lounge`; do not merge PR #104. Bump `frontend/src/utils/hardDataUrls.ts` stamp. Verify `cd frontend && CI=true npx react-scripts test --watchAll=false --testPathPattern='lalem|Lalem|shuileme|Shuileme|catalog.test'` and `cd backend && go test ./internal/ai ./internal/api ./internal/database -count=1` pass. Confirm `unset CI && cd frontend && npm run build` succeeds (no new iterator-spread TS2802)
