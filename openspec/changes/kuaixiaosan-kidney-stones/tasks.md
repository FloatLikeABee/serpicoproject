# Tasks

## 1. Failing contracts (TDD — no production page yet)

- [ ] 1.1 Flip frontend contracts in `frontend/src/pages/Kuaixiaosan.test.tsx`, `frontend/src/App.publicRoutes.test.tsx`, and `frontend/src/kuaixiaosanAbsence.test.ts`: public `/kuaixiaosan` heading 肾结石快消散; six `.kx-dock` buttons 石/例/复/典/影/聊; `/kuaixiaosan/chat` unauthenticated shows `textarea` inside 肾结石快消散 not officer chrome; 聊 `pushState`s `/kuaixiaosan/chat` and 石 returns `/kuaixiaosan`; delayed mock `POST /kuaixiaosan/chat` shows `role="status"` then a recovery reply that asks a feeling or names a step without `you have` / `你患有`; no fetch to `/lalem/` or `/shuileme/`; 好了 freezes 已看; sequential gallery titles before every `src`; sheet hero `/kuaixiaosan/stones/*.jpg`; no sit-alert at 5 min; no `video` / `Notification` / wikipedia `<a>` / `#c6a56a` / `ll-world` / `sm-world`. Use `Array.from`. Verify `cd frontend && CI=true npx react-scripts test --watchAll=false --testPathPattern='Kuaixiaosan.test|App.publicRoutes|kuaixiaosanAbsence'` **fails** while the page and nested chat route are missing
- [ ] 1.2 Flip Go contracts in `backend/internal/ai/kuaixiaosan_catalog_test.go`, `backend/internal/ai/kuaixiaosan_chat_test.go`, and `backend/internal/api/kuaixiaosan_handlers_test.go`: `GET /api/v1/kuaixiaosan/stones` (≥16, local `/kuaixiaosan/stones/`, composition/site filters); cases ≥12; recover ≥12; lore ≥16; imaging ≥12; unique JPEG SOI hashes; no `://` in `imageUrl`; lore/recover/cases bodies have sources and no `you have` / `你患有`; `GET /wiki` allowlist; `POST /api/v1/kuaixiaosan/chat` 200 `{reply}`; canned when no `CompleteFn`; diagnosis stripped; reply must not be 拉了么 `便便科普` or 睡了么 commutative-addition canned as the primary voice. Verify `cd backend && go test ./internal/ai ./internal/api -count=1` **fails** while the routes and catalogs are missing

## 2. Catalogs, media, API

- [ ] 2.1 Embed `kuaixiaosan_stones.json`, `kuaixiaosan_cases.json`, `kuaixiaosan_recover.json`, `kuaixiaosan_lore.json`, `kuaixiaosan_imaging.json` with the locked filter vocab and bilingual encyclopedia copy (cases third-person vignettes; recover includes fluids/rest/strain/ER steps). Verify catalog unit tests for floors, unique ids, local prefixes, and hygiene
- [ ] 2.2 Ship unique real Wikimedia Commons / public-domain JPEGs under `frontend/public/kuaixiaosan/{stones,cases,recover,lore,imaging}/` plus `ATTRIBUTION.md` (no AI-generated stills, no identifiable patient faces, no hotlinks). Verify unique-hash tests pass and every `imageUrl` exists on disk
- [ ] 2.3 Add `AdviseKuaixiaosanChat` (SiliconFlow `CompleteFn`, feeling-then-recovery prompt, parse `{reply}` or prose, ≥8 canned recovery lines per locale, strip diagnosis). Wire `GET /api/v1/kuaixiaosan/stones|cases|recover|lore|imaging|wiki` and `POST /api/v1/kuaixiaosan/chat` with a **separate** limiter. Reuse wiki allowlist helpers; do not call `/lalem/*` or `/shuileme/*` from this client. Verify 1.2 Go tests pass and existing lalem/shuileme/fridge-raid tests still pass

## 3. Page, routes, chat dock

- [ ] 3.1 Add i18n `kuaixiaosan.*` (title 肾结石快消散, kicker 快消散, docks 石/例/复/典/影/聊, stop 好了, EN counterparts) and `serpico.kuaixiaosan.lang` default **cn**. Verify catalog + lang-helper tests
- [ ] 3.2 Implement `Kuaixiaosan.tsx`: `html.kx-world`, quiet 已看 clock, header 好了 freeze, six docks, `queueLoungePhotos` on galleries, photo sheets `.kx-sheet-hero` + chips, in-lounge wiki buttons, disclaimers, chat transcript + textarea POSTing only to `/kuaixiaosan/chat`, `.kx-think` `role="status"` `aria-busy`, `goDock` / `dockFromPath` for `/kuaixiaosan/chat`. CSS tokens `--kx-bg: #12181c` etc., dock `repeat(6, minmax(0,1fr))` wrap `<339px`. Verify 1.1 Kuaixiaosan tests pass
- [ ] 3.3 Register `/kuaixiaosan/chat` then `/kuaixiaosan` in `App.tsx` outside `ProtectedRoute`; add `'kuaixiaosan'` and `'kuaixiaosan/chat'` to `spa-routes.js`. Verify `kuaixiaosanAbsence` and the public-routes `/kuaixiaosan/chat` test pass

## 4. Isolation

- [ ] 4.1 Isolation: no officer Navigation / Fridge Raid / 拉了么 / 睡了么 restyle; this page has no `/lalem/` or `/shuileme/` client calls; no `Notification` / `requestPermission`; no `<video>`; no `#c6a56a` / `#ff4d8d` / `#7ee0ff` / `#c9f07a` in `html.kx-world`; no sit-alert/companion/拉榜/sound/wind-down on this page. Do not merge PR #104. Do not archive existing lounge changes. Bump `frontend/src/utils/hardDataUrls.ts` stamp. Verify `cd frontend && CI=true npx react-scripts test --watchAll=false --testPathPattern='kuaixiaosan|Kuaixiaosan|shuileme|Shuileme|lalem|Lalem|catalog.test|App.publicRoutes'` and `cd backend && go test ./internal/ai ./internal/api ./internal/database -count=1` pass. Confirm `unset CI && cd frontend && npm run build` succeeds (spa-routes writes `kuaixiaosan/chat/index.html`, no new iterator-spread TS2802)
